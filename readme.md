React frontend + postgres db + node js(express)
Feature:
* Create text paste with expiration setting, visibility setting(public, private, unlisted)
* Search public paste
* Download/copy paste
* Auto delete expired paste via cron job

Start up with
```
npm run dev
```