React frontend + postgres db + node js(express)
Feature:
* Create text paste with expiration setting, visibility setting(public, unlisted)
* Search public paste
* Download/copy paste
* Auto delete expired paste via cron job

TODO: Authentication, User related page

Start up with
```
npm run dev
```